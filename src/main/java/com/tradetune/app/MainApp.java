package com.tradetune.app;

import com.tradetune.app.domain.model.Client;
import com.tradetune.app.domain.repository.ClientRepository;
import com.tradetune.app.infrastructure.persistance.ClientRepositoryImpl;
import com.tradetune.app.util.HibernateUtil;
import org.hibernate.Session;

import java.time.LocalDate;
import java.util.Optional;

public class MainApp {

    public static void main(String[] args) {
    }
}